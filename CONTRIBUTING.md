# 기여 안내

작은 변경도 먼저 이슈에서 목적과 서비스 부하 영향을 설명해 주세요. 실제 네이버 상품이나
실제 알림 채널을 반복 호출하는 테스트는 받지 않습니다. 테스트는 fake 또는 mock을 사용해야
하며, CAPTCHA·접근 제한·자동화 탐지를 우회하는 코드는 제안하지 마세요.

개발 환경을 준비한 뒤 다음 검사를 모두 통과시켜 주세요.

```bash
python -m pip install -e ".[dev]"
ruff format --check .
ruff check .
mypy src
pytest
```

커밋 전에 `.env`, 로그, 상태 파일, 쿠키, API 응답 원본과 개인 경로가 포함되지 않았는지
확인해 주세요.
